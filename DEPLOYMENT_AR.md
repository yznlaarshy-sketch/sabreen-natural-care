# دليل التجهيز والاستضافة — صابرين بشير للعناية الطبيعية

هذه النسخة مجهزة لتخزين حالة المتجر الرئيسية في Supabase بدل الاعتماد على ملف JSON محلي.

## قبل التشغيل
ضع في إعدادات الاستضافة متغيري البيئة التاليين فقط:

- `SUPABASE_URL` = رابط مشروع Supabase.
- `SUPABASE_SERVICE_ROLE_KEY` = المفتاح السري للخادم (Secret/Service Role Key حسب واجهة Supabase).

لا تضع المفتاح السري في `src/` أو في أي كود يعمل داخل المتصفح، ولا ترسله لأي شخص.

إذا كانت ميزات الذكاء الاصطناعي مستخدمة، أضف `GEMINI_API_KEY` أيضاً كمتغير سري.

## البناء والتشغيل
- Build: `npm run build`
- Start: `npm start`

## قاعدة البيانات
يجب تنفيذ `supabase/schema.sql` في مشروع Supabase قبل التشغيل.

عند أول تشغيل، إذا كان جدول `store_state` فارغاً، يستخدم الخادم `data/store_seed.json` لنسخ حالة المتجر الحالية إلى Supabase. بعد ذلك تصبح Supabase هي المصدر الدائم للبيانات.

## ملاحظة مهمة
ملف `data/store_seed.json` يحتوي نسخة من بيانات المتجر الحالية ويستخدم فقط كبذرة أولية على الخادم. لا تحذف جدول `store_state` بعد أول تشغيل إذا كنت تريد الاحتفاظ بالبيانات التي تم تعديلها من لوحة الإدارة.
