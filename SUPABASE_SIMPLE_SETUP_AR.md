# أسهل طريقة لتشغيل موقع صابرين مع قاعدة بيانات مجانية

## 1) في Supabase
- أنشئ مشروعًا مجانيًا.
- افتح SQL Editor.
- انسخ كل محتوى `supabase/schema.sql` والصقه ثم اضغط Run.

## 2) خذ معلومات الاتصال
من Project Settings → API:
- Project URL
- service_role key

لا ترسل `service_role key` لأي شخص ولا تضعه في كود المتصفح.

## 3) في الاستضافة
أضف متغيري البيئة:
- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY`

ثم أعد تشغيل/نشر الموقع.

### ماذا سيحدث؟
- الموقع الحالي يبقى كما هو.
- إذا كانت قاعدة Supabase فارغة، السيرفر ينقل البيانات الموجودة حاليًا في `data/store_data.json` إليها تلقائيًا.
- بعد ذلك، أي إضافة/تعديل/حذف من لوحة الإدارة تُحفظ في Supabase.
- الطلبات وإيصالات التحويل والإعدادات تبقى محفوظة.
- `data/store_data.json` يبقى كنسخة احتياطية محلية.

لا تحتاج إلى إنشاء جداول منتجات أو طلبات متعددة؛ هذا التصميم متعمد ليكون أسهل وأقل عرضة لكسر الموقع الحالي.
